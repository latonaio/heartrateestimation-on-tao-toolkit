version: "3"
services:
  heartrate-tao-toolkit:
    container_name: heartrate-tao-toolkit
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - ./:/app/src
    working_dir: /app/src
    tty: true
